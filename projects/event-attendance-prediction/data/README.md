# Data — digitized from the EVE article (Oliveira, 2016)

All files were transcribed by hand from the tables of the article in
[`../source-material/`](../source-material/). Real data, small n — treat as
seed/validation data, not training data.

| File | Source tables | Content |
|---|---|---|
| `events_by_size.csv` | 7–10 | 16 public events classified by size (micro/small/medium/large): RSVP counts and actual attendance |
| `experiment_pre_eve.csv` | 19 | 3 invitation rounds for the percussion-group rehearsals, before EVE was used |
| `experiment_eve.csv` | 22, 25, 27, 28 | The 4-rehearsal field experiment (May 2013): RSVP counts, attendance per response type, expected proportions, EVE's prediction and actual attendance |
| `external_effects.csv` | 23, 28 | External effects (± % influence) associated with each experiment date |
| `model_coefficients.json` | — | Generated by `src/model.py`; embedded into `app/` and `reports/` |

## Column vocabulary

- Size classes: `micro` ≤ 200 · `small` 201–1500 · `medium` 1501–5500 · `large` ≥ 5501 (table 6).
- RSVP response types: `confirmed`, `maybe`, `declined`, `no_reply`.
- `invited` = total guests · `attendance` = people actually present.

### `experiment_eve.csv`

- `{type}_responses` = guests who gave that response; `{type}_present` = how many
  of them actually showed up (table 25).
- `prop_{type}` = the attendance proportion per response type that the operator
  fed to EVE for that event (table 27).
- `article_prediction` / `article_attendance` = EVE's prediction and the
  attendance as reported in table 28.

## Known inconsistencies in the source

The article's arithmetic does not always close; kept as printed, flagged here
(and asserted in `tests/test_data.py` so nobody "fixes" them silently):

1. **Table 28, event 2013-05-20:** expected confirmed printed as 16, but
   `31 × 0.90 = 27.9`. The printed value matches the *previous* proportion
   (≈50%), suggesting a stale value. Consequently the prediction (23) is not
   reproducible from tables 25+27.
2. **Table 28, event 2013-05-26:** expected confirmed printed as 29
   (`35 × 0.85`), while table 27 states 90% for that date.
3. **Table 28 vs 25, event 2013-05-26:** attendance printed as 35 in table 28,
   but per-response attendance in table 25 sums to 37 (32+2+0+3).
4. **Effect application:** predictions for 05-05/05-12 fit effects applied
   *additively* (sum of percentages) with truncation, while 05-20/05-26 only
   fit if a single −5% is applied. No single rule reproduces all four rows.

`src/baseline.py` implements one clean interpretation of the formula and
reports the deviation from the article's own predictions instead of forcing
agreement.
