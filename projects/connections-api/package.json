{
  "name": "connections-api",
  "version": "0.1.0",
  "private": true,
  "description": "CRUD REST API over the Conexões da História nodes dataset, for API test practice (Node + Express + Playwright).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
