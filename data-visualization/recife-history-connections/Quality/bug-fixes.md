# Quality — Bug Fixes

Log of defects found and fixed during testing of the Recife History Connections site.
All entries below are from the **2026-07-02** testing session (times in the Date column, 24h).

| Page(s) / source | Affected pages (scope) | Component affected | Component type | Description | What happened | What was done | Commit | Date |
|---|---|---|---|---|---|---|---|---|
| `mobile/site/*.html`, `node/*.html` | All 5 mobile app screens (Início, Lista, Matriz, Sobre, Favoritos) + 567 node detail pages | Progressive-enhancement visibility (`.js-only` / `.no-js-only`) | CSS + generator (`sitegen.py`) | JS-only chrome (e.g. the Favoritos button) must show only when JS runs, and no-JS hints only when it doesn't. | The `.has-js .js-only { display: initial }` rule clobbered each element's own layout (`flex`/`block`), so enhanced controls rendered wrong. | Switched to `html.no-js .js-only { display:none !important }` and `html.has-js .no-js-only { display:none !important }`, preserving each element's own display. | `5362978` | 2026-07-02 08:54 |
| `mobile/build/sitegen.py`, `assets/app.css`, all mobile pages | All 6 mobile app screens (Início, Lista, Grafo, Matriz, Sobre, Favoritos) + 567 node pages | Type accent colors (`--type-*-ink`); page `<head>`; list card `aria-label` | CSS + generator (`sitegen.py`) | Lighthouse pass — accessibility / best-practices cleanup. | (1) Text-on-tint contrast for Person / Historical-fact inks was below AA; (2) favicon request 404'd; (3) list cards had an `aria-label` duplicating the visible name ("label in name"). | (1) Darkened `--type-person-ink` / `--type-historical_fact-ink`; (2) added inline `<link rel='icon' href='data:,'>`; (3) removed the redundant `aria-label` so the accessible name comes from visible text. | `d546f2d` | 2026-07-02 10:09 |
| `mobile/build/sitegen.py`, `mobile/site/node/*.html` | 466 node detail pages that carry an image | Node detail image (`.detail-img`) | HTML generator (`sitegen.py`) | Nodes with a missing/broken image URL should not show a broken-image icon. | When the image URL failed to load, the browser rendered its default broken-image placeholder. | Added `onerror="this.style.display='none'"` so a broken image is simply hidden, leaving the title/text. | `c4218f4` | 2026-07-02 11:03 |
| `pages/stats.html` | Fill Rate page (desktop) | Entry-row rendering (name field) | JavaScript (DOM render) | An entry with an empty name should not render the literal text `undefined`. | Rows for records with no name printed `undefined` in the list. | Added a guard: `(nome \|\| '—')` so empty names render an em-dash. | `b953444` | 2026-07-02 11:47 |
| `pages/matrix.html`, `pages/diagram.html`, `pages/sources.html`, `pages/stats.html` + `pages/en/` counterparts | 8 desktop pages: Matrix, Diagram, Fontes, Fill Rate (PT + EN) | Viewport-based routing (`matchMedia('(max-width:1023px)')` redirect) | Desktop → mobile responsive routing script | On narrow screens the desktop pages should redirect to the mobile app; only `index.html` did. | Shrinking the window (or opening on a narrow screen) on Matrix, Diagram, Fontes or Fill Rate stayed on the desktop page — the switch to mobile never happened. | Added the viewport-routing script to all four desktop pages (PT + EN), each pointing to its corresponding mobile page: matrix→matriz, diagram→graph, sources→fontes, stats→fillrate. | uncommitted (working tree) | 2026-07-02 (this session) |
| `mobile/site/matriz.html`, `graph.html`, `fillrate.html`, `fontes.html` + `mobile/build/sitegen.py` | 4 mobile pages: Matriz, Grafo, Fill rate, Fontes | Mobile → desktop expand redirect (`matchMedia('(min-width:1024px)')`) | Mobile → desktop responsive routing script | When a mobile page is open and the window is expanded, it should land on the matching desktop page. | Every mobile page redirected to desktop `index.html` instead of its counterpart (e.g. Matriz → `index.html` rather than `matrix.html`). Same for Fill rate, Fontes and Sobre. | Added a `DESKTOP_FOR_PAGE` map in `sitegen.py` so each page redirects to its real counterpart (matriz→matrix, graph→diagram, fillrate→stats, fontes→sources) and regenerated the site. Início, list, Sobre and node pages fall back to `index.html` (no desktop counterpart). | uncommitted (working tree) | 2026-07-02 (this session) |
